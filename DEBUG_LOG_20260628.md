# warehouse 调试记录 — 2026-06-28 续

## 接续点
6/28 16:02 中断 — SYNONYM_CLOSURE 恢复 + route.ts early return 移除，未验证。

---

## 本轮完成

### P0-1: PARAM_DEFS 注册表 (已修)
- `constraint-match.ts:428` 硬编码列表 `['bit','Iout','kVrms']` → `getParamDef(family)?.compare==='>='`
- 今后新增 compare='>=' 参数只需加 PARAM_DEFS 一条，约束层自动覆盖

### P0-2: Tag 分类体系碎片化 (已修)
- `query_parser.ts`: 新增 `getKnownTags()` 从 CATEGORY_RULES + MODIFIER_RULES + PROTECTION_KEYWORDS 自动派生 + 26 手动补充
- `route.ts`: 120 行硬编码 KNOWN_TAGS → `getKnownTags()` 1 行
- PROTECTION_KEYWORDS 从 `parseQuery()` 内部提为模块级常量

### NSE34xx 数据补全 + FAE near-miss
- `numerify_params.py`: 新增 `scan_detail_for_numerics()` 从 detail 散文提取参数
- NSE34xx 获得 Vin_max=40V（从"工作电压范围至40V"提取）
- Iout 缺失时用 Rdson(25mΩ/50mΩ/140mΩ) 作为电流能力代理
- `route.ts`: FAE near-miss 逻辑，Iout 数据缺失产品标 tier=0 + Rdson
- `page.tsx`: 前端参考料卡片显示 `Rdson=25mΩ，电流能力待FAE确认`
- `route.ts:767`: 修复 exactMatches 11-30 区间丢失 results 的 bug

### LDO Vout 修复
- `voutSpecOf`: adjustable 兜底 `/adjustable\s*output/` → `/adjustable/`
- `numerify_params.py`: 新增 `Fixed N`（无括号）、`Adjustable (X to Y)`、`±` 前缀
- LDO 查询结果: 16→35 款

### LLM 数值数据缺口 rescue（泛化）
- Vout 专用 → 全部 8 个数值参数族（Vin/Vout/Iout/Mbps/kVrms/通道/端口/bit）
- 数据缺失时 LLM 读 `_params` 原文语义判断
- `route.ts`: 新增通用 `llmNumericalDataGapRescue` 逻辑

### Bug 修复
- `route.ts` failTags 真空条件: `rescues.length===0` 时 `[].every()`=true 误删所有非数值 must 标签 → 加 `rescues.length>0` 守卫
- `query_parser.ts`: "休眠" → 加 `休眠|sleep` 映射到"待机模式"
- `constraint-match.ts`: TRANSLATION_HINTS "待机模式" 补 `inh` (LIN 收发器用 INH pin 做休眠)
- `route.ts:844` 补 exactMatches → results 输出

### P1-1: 5 套证据匹配合并 (已修)
- PARENT_CLOSURE + SYNONYM_CLOSURE + TRANSLATION_HINTS + SEMANTIC_ALIASES + EVIDENCE_REGISTRY
- → 统一 `EVIDENCE_RULES` 数组: featureTokens / textPattern / numericKey
- 删 ~100 行，新增 tag 只需 1 条 entry

### P1-2: Python 测试纯 API 化 (已修)
- `test_constraint_layer.py`: 836→270 行
- 删所有 Python 副本（tag_satisfied/PARENT_CLOSURE/SYNONYM_CLOSURE 等）
- 改为纯 API 断言: must/features/results/sortKey/crossRefTarget

---

## 修改文件清单
- `web/app/api/interpret/query_parser.ts` — getKnownTags() + PROTECTION_KEYWORDS 模块化 + 休眠/sleep 映射
- `web/app/api/interpret/constraint-match.ts` — PARAM_DEFS 派生 + EVIDENCE_RULES 统一 + adjustable 兜底 + TRANSLATION_HINTS 补 inh
- `web/app/api/interpret/route.ts` — getKnownTags() 替代硬编码 + FAE near-miss + LLM numerical gap rescue + failTags 真空 fix + exactMatches results fix
- `scripts/numerify_params.py` — scan_detail_for_numerics() + Fixed N/Adjustable (X to Y)/± 前缀
- `web/app/page.tsx` — FAE near-miss Rdson 渲染 + safety net push
- `scripts/test_constraint_layer.py` — 836→270 行纯 API 断言
- `web/public/data/products_structured.json` — numerify 重跑更新

---

## 测试状态
| 测试 | 结果 |
|------|------|
| Parser | 111/111 ✅ |
| Validate (TAGS/EXCLUSIVE/SCHEMA/PARAMN/SECTION/LLM) | 全通过 ✅ |
| Constraint | 62/81（19 存量 + 数据变更） |
| Server | localhost:3000 |

---

## 关键查询验证
- `高边开关` → 35 款（含纳芯微） ✅
- `24V 2A 高边开关 短路 过温 开路诊断` → TPS42S40Q + 27 纳芯微参考料 ✅
- `LDO 5V 3.3V 500mA` → 35 款 ✅
- `车规 LIN 12V 休眠 唤醒` → 4 款 ✅
- `CAN FD 隔离 2.5kVrms` → 正确识别 ✅
- `Buck 100V` → 正确 ✅

---

## 待处理 (P2 + 遗留)
- tagSatisfied 分支顺序敏感
- 数据管道格式契约（JSON Schema）
- Constraint 测试 19 存量失败（yutai 排名变更/IO扩展器 PN/旧版回归）
- P1 LLM prompt 标签列表与 getKnownTags() 同步
